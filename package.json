{
  "name": "east-railway-hub",
  "version": "1.2.0",
  "private": true,
  "description": "EAST Railway Hub \u2014 WS relay + Phase-2 HTTP gateway to east-validator. Not part of consensus; proxies /rpc/* to the chain sealer.",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node --max-old-space-size=350 dist/server.js",
    "dev": "ts-node src/server.ts"
  },
  "dependencies": {
    "ws": "^8.18.0",
    "typescript": "^5.5.0",
    "@types/node": "^20.14.0",
    "@types/ws": "^8.5.10"
  },
  "devDependencies": {
    "ts-node": "^10.9.2"
  }
}
